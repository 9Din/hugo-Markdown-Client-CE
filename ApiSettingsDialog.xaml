<Window x:Class="Huge.ApiSettingsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="DeepSeek API 设置"
        Width="480" MinWidth="380" MaxWidth="600"
        Height="Auto"
        MaxHeight="560"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        ShowInTaskbar="False"
        Background="{DynamicResource BgBrush}"
        Foreground="{DynamicResource TextBrush}">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Border Background="{DynamicResource BgBrush}" Padding="0">
            <StackPanel Margin="16">
                <TextBlock Text="DeepSeek API 设置" FontSize="16" FontWeight="Bold"
                           Foreground="{DynamicResource TextBrush}" Margin="0,0,0,12"/>

                <TextBlock Text="Base URL" FontSize="12" Margin="0,6,0,2"
                           Foreground="{DynamicResource TextBrush}"/>
                <TextBox x:Name="BaseUrlBox" FontFamily="Consolas" FontSize="12"
                         Background="{DynamicResource EditorBgBrush}"
                         Foreground="{DynamicResource TextBrush}"
                         BorderBrush="{DynamicResource BorderBrush}"
                         CaretBrush="{DynamicResource TextBrush}"/>

                <TextBlock Text="API Key" FontSize="12" Margin="0,10,0,2"
                           Foreground="{DynamicResource TextBrush}"/>
                <PasswordBox x:Name="ApiKeyBox" FontFamily="Consolas" FontSize="12" PasswordChar="●"
                             Background="{DynamicResource EditorBgBrush}"
                             Foreground="{DynamicResource TextBrush}"
                             BorderBrush="{DynamicResource BorderBrush}"
                             CaretBrush="{DynamicResource TextBrush}"/>

                <TextBlock x:Name="ApiKeyHintText" FontSize="11" Foreground="{DynamicResource MutedTextBrush}" Text="可在 https://platform.deepseek.com 获取 API Key" Margin="0,4,0,0" TextWrapping="Wrap"/>

                <TextBlock Text="Model" FontSize="12" Margin="0,10,0,2"
                           Foreground="{DynamicResource TextBrush}"/>
                <!-- 非可编辑 ComboBox：DeepSeek 仅 deepseek-chat / deepseek-reasoner 两个模型，
                     使用完整主题化模板，避免可编辑模式内部系统白底 TextBox 导致选择失效 -->
                <ComboBox x:Name="ModelBox" FontSize="12">
                    <ComboBox.Template>
                        <ControlTemplate TargetType="ComboBox">
                            <Grid>
                                <!-- 整框底色+边框（放最底层） -->
                                <Border x:Name="wholeBorder"
                                        Background="{DynamicResource EditorBgBrush}"
                                        BorderBrush="{DynamicResource BorderBrush}"
                                        BorderThickness="1"
                                        CornerRadius="4"/>
                                <!-- 选中项显示 -->
                                <ContentPresenter x:Name="contentSite"
                                                  Margin="8,0,30,0"
                                                  VerticalAlignment="Center"
                                                  IsHitTestVisible="False"
                                                  Content="{TemplateBinding SelectionBoxItem}"
                                                  ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"/>
                                <!-- 下拉箭头按钮 -->
                                <ToggleButton x:Name="toggleBtn"
                                              Focusable="False"
                                              ClickMode="Press"
                                              HorizontalAlignment="Right"
                                              Width="26"
                                              IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                                    <ToggleButton.Template>
                                        <ControlTemplate TargetType="ToggleButton">
                                            <Border x:Name="toggleBd"
                                                    Background="{DynamicResource EditorBgBrush}"
                                                    BorderBrush="{DynamicResource BorderBrush}"
                                                    BorderThickness="0,1,1,1"
                                                    CornerRadius="0,4,4,0">
                                                <Path Data="M 0 0 L 4 4 L 8 0 Z"
                                                      Fill="{DynamicResource MutedTextBrush}"
                                                      HorizontalAlignment="Center"
                                                      VerticalAlignment="Center"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="toggleBd" Property="Background" Value="{DynamicResource BorderBrush}"/>
                                                </Trigger>
                                                <Trigger Property="IsChecked" Value="True">
                                                    <Setter TargetName="toggleBd" Property="Background" Value="{DynamicResource BorderBrush}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </ToggleButton.Template>
                                </ToggleButton>
                                <!-- 下拉列表 -->
                                <Popup x:Name="PART_Popup"
                                       IsOpen="{TemplateBinding IsDropDownOpen}"
                                       Placement="Bottom"
                                       AllowsTransparency="True"
                                       PopupAnimation="Slide"
                                       StaysOpen="False">
                                    <Border x:Name="DropDownBorder"
                                            Background="{DynamicResource PanelBrush}"
                                            BorderBrush="{DynamicResource BorderBrush}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource AncestorType=ComboBox}}">
                                        <ScrollViewer MaxHeight="180">
                                            <ItemsPresenter/>
                                        </ScrollViewer>
                                    </Border>
                                </Popup>
                            </Grid>
                        </ControlTemplate>
                    </ComboBox.Template>
                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="Background" Value="{DynamicResource PanelBrush}"/>
                            <Setter Property="Foreground" Value="{DynamicResource TextBrush}"/>
                            <Setter Property="Padding" Value="8,4"/>
                            <Style.Triggers>
                                <Trigger Property="IsHighlighted" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource AccentBrush}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ComboBox.ItemContainerStyle>
                    <ComboBoxItem Content="deepseek-chat"/>
                    <ComboBoxItem Content="deepseek-reasoner"/>
                </ComboBox>

                <TextBlock x:Name="ModelHintText" FontSize="11" Foreground="{DynamicResource MutedTextBrush}" Text="deepseek-chat：通用对话；deepseek-reasoner：深度推理" Margin="0,4,0,0" TextWrapping="Wrap"/>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,16,0,0">
                    <Button x:Name="TestBtn" Content="测试连接" Padding="12,6" Margin="0,0,8,0"/>
                    <Button x:Name="OkBtn" Content="确定" Padding="16,6" Width="80" Margin="0,0,8,0" IsDefault="True"/>
                    <Button x:Name="CancelBtn" Content="取消" Padding="16,6" Width="80" IsCancel="True"/>
                </StackPanel>
            </StackPanel>
        </Border>
    </ScrollViewer>
</Window>